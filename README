unveil
======

Program written in POSIX shell to store and decrypt secrets.

It uses Sequoia SQ as its backend.

Installation
------------

	# make install

Usage
-----

The SECRETS environment variable is used to locate the directory containing the
encrypted secrets. It defaults to "$HOME/secrets".

	Usage: unveil [-u secret | -p secret | -o secret | -e secret]
